import {readFileSync,writeFileSync} from 'node:fs';const d=JSON.parse(readFileSync(new URL('../data/content.json',import.meta.url)));const pictures=new Set([...d.mode2,...d.mode5].flatMap(r=>Object.values(r.images)).concat(d.times.map(t=>t.image)));writeFileSync(new URL('../image-catalog.md',import.meta.url),'# Εικόνες της εφαρμογής\n\n'+[...pictures].sort().map(p=>`- [${p.split('/').pop()}](${p})`).join('\n')+'\n');console.log(pictures.size+' referenced images');
